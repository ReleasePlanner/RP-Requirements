name: Test SSH Connection

on:
  workflow_dispatch:

jobs:
  test-ssh:
    name: Test SSH Connection to VPS
    runs-on: ubuntu-latest
    steps:
      - name: Install SSH tools
        run: |
          sudo apt-get update
          sudo apt-get install -y openssh-client sshpass || true

      - name: Debug Secrets
        run: |
          echo "🔍 Checking secrets..."
          echo "VPS_HOST configured: ${{ secrets.VPS_HOST != '' }}"
          echo "VPS_USER configured: ${{ secrets.VPS_USER != '' }}"
          echo "VPS_SSH_KEY configured: ${{ secrets.VPS_SSH_KEY != '' }}"
          echo "VPS_SSH_PASSWORD configured: ${{ secrets.VPS_SSH_PASSWORD != '' }}"
          
          if [ -z "${{ secrets.VPS_HOST }}" ]; then
            echo "❌ VPS_HOST secret is not configured"
            exit 1
          fi
          
          if [ -z "${{ secrets.VPS_USER }}" ]; then
            echo "⚠️ VPS_USER not configured, will use 'root'"
          fi

      - name: Setup SSH Key (only if no password)
        if: secrets.VPS_SSH_KEY != ''
        continue-on-error: true
        uses: webfactory/ssh-agent@v0.9.0
        with:
          ssh-private-key: ${{ secrets.VPS_SSH_KEY }}

      - name: Test SSH Connection
        run: |
          VPS_HOST="${{ secrets.VPS_HOST }}"
          VPS_USER="${{ secrets.VPS_USER || 'root' }}"
          
          echo "🔐 Testing SSH connection to $VPS_USER@$VPS_HOST..."
          
          # Priorizar contraseña si está configurada (más confiable cuando está configurada)
          if [ -n "${{ secrets.VPS_SSH_PASSWORD }}" ]; then
            echo "🔑 Using SSH password authentication..."
            
            # Deshabilitar agente SSH para evitar intentar usar claves
            unset SSH_AUTH_SOCK
            unset SSH_AGENT_PID
            
            sshpass -p '${{ secrets.VPS_SSH_PASSWORD }}' \
              ssh -o StrictHostKeyChecking=no \
                  -o UserKnownHostsFile=/dev/null \
                  -o ConnectTimeout=10 \
                  -o PreferredAuthentications=password \
                  -o PubkeyAuthentication=no \
                  -o PasswordAuthentication=yes \
                  -o BatchMode=yes \
                  -o NumberOfPasswordPrompts=1 \
                  -o IdentitiesOnly=yes \
                  -v \
                  $VPS_USER@$VPS_HOST \
                  "echo '✅ SSH connection successful!' && uname -a && docker --version 2>/dev/null || echo '⚠️ Docker not installed'" || {
              echo "❌ SSH connection test failed with password"
              echo "💡 Verify VPS_SSH_PASSWORD is correct in GitHub Secrets"
              echo "💡 Verify VPS_USER is correct (usually 'root')"
              exit 1
            }
          elif [ -n "${{ secrets.VPS_SSH_KEY }}" ]; then
            echo "🔑 Using SSH key authentication..."
            echo "📋 Verifying SSH key format..."
            if ! ssh-add -l 2>/dev/null | grep -q "The agent has no identities"; then
              echo "✅ SSH key loaded in agent"
              ssh-add -l
            else
              echo "⚠️ SSH key not loaded in agent"
            fi
            
            ssh -v -o StrictHostKeyChecking=no \
                -o UserKnownHostsFile=/dev/null \
                -o ConnectTimeout=10 \
                $VPS_USER@$VPS_HOST \
                "echo '✅ SSH connection successful!' && uname -a && docker --version 2>/dev/null || echo '⚠️ Docker not installed'" || {
              echo "❌ SSH connection test failed with SSH key"
              echo "💡 Troubleshooting steps:"
              echo "   1. Verify VPS_SSH_KEY in GitHub Secrets includes BEGIN and END lines"
              echo "   2. Verify the public key is in ~/.ssh/authorized_keys on the VPS"
              echo "   3. Check file permissions: chmod 600 ~/.ssh/authorized_keys"
              echo "   4. Verify VPS_USER is correct (usually 'root')"
              echo "   5. Try using VPS_SSH_PASSWORD instead for testing"
              exit 1
            }
          else
            echo "❌ No SSH credentials provided"
            echo "💡 Please configure either VPS_SSH_KEY or VPS_SSH_PASSWORD in GitHub Secrets"
            exit 1
          fi
          echo "✅ SSH connection test successful"

      - name: Test Docker Installation
        run: |
          VPS_HOST="${{ secrets.VPS_HOST }}"
          VPS_USER="${{ secrets.VPS_USER || 'root' }}"
          
          echo "🐳 Testing Docker installation..."
          
          # Priorizar contraseña si está configurada
          if [ -n "${{ secrets.VPS_SSH_PASSWORD }}" ]; then
            unset SSH_AUTH_SOCK
            unset SSH_AGENT_PID
            
            sshpass -p '${{ secrets.VPS_SSH_PASSWORD }}' \
              ssh -o StrictHostKeyChecking=no \
                  -o UserKnownHostsFile=/dev/null \
                  -o PreferredAuthentications=password \
                  -o PubkeyAuthentication=no \
                  -o PasswordAuthentication=yes \
                  -o BatchMode=yes \
                  -o NumberOfPasswordPrompts=1 \
                  -o IdentitiesOnly=yes \
                  $VPS_USER@$VPS_HOST \
                  "docker --version && docker-compose --version || echo '⚠️ Docker Compose not installed'"
          elif [ -n "${{ secrets.VPS_SSH_KEY }}" ]; then
            ssh -o StrictHostKeyChecking=no -o UserKnownHostsFile=/dev/null \
              $VPS_USER@$VPS_HOST \
              "docker --version && docker-compose --version || echo '⚠️ Docker Compose not installed'"
          fi

      - name: Test Directory Permissions
        run: |
          VPS_HOST="${{ secrets.VPS_HOST }}"
          VPS_USER="${{ secrets.VPS_USER || 'root' }}"
          
          echo "📁 Testing directory permissions..."
          
          # Priorizar contraseña si está configurada
          if [ -n "${{ secrets.VPS_SSH_PASSWORD }}" ]; then
            unset SSH_AUTH_SOCK
            unset SSH_AGENT_PID
            
            sshpass -p '${{ secrets.VPS_SSH_PASSWORD }}' \
              ssh -o StrictHostKeyChecking=no \
                  -o UserKnownHostsFile=/dev/null \
                  -o PreferredAuthentications=password \
                  -o PubkeyAuthentication=no \
                  -o PasswordAuthentication=yes \
                  -o BatchMode=yes \
                  -o NumberOfPasswordPrompts=1 \
                  -o IdentitiesOnly=yes \
                  $VPS_USER@$VPS_HOST \
                  "mkdir -p /opt/modules/requirements-management && ls -la /opt/modules && echo '✅ Directory accessible'"
          elif [ -n "${{ secrets.VPS_SSH_KEY }}" ]; then
            ssh -o StrictHostKeyChecking=no -o UserKnownHostsFile=/dev/null \
              $VPS_USER@$VPS_HOST \
              "mkdir -p /opt/modules/requirements-management && ls -la /opt/modules && echo '✅ Directory accessible'"
          fi

