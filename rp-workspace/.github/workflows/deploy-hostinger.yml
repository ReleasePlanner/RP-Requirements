name: Deploy to Hostinger VPS

on:
  push:
    branches:
      - main
      - develop
  workflow_dispatch:
    inputs:
      environment:
        description: "Environment to deploy"
        required: true
        type: choice
        options:
          - development
          - production
      skip_tests:
        description: "Skip tests (use with caution)"
        required: false
        type: boolean
        default: false

env:
  NODE_VERSION: "20.x"
  VPS_HOST: ${{ secrets.VPS_HOST }}
  VPS_USER: ${{ secrets.VPS_USER || 'root' }}
  VPS_SSH_KEY: ${{ secrets.VPS_SSH_KEY }}
  VPS_SSH_PASSWORD: ${{ secrets.VPS_SSH_PASSWORD }}
  MODULE_NAME: requirements-management
  MODULE_PATH: /opt/modules/${{ env.MODULE_NAME }}

jobs:
  integrity-check:
    name: Pre-Deployment Integrity Check
    runs-on: ubuntu-latest
    if: github.event.inputs.skip_tests != 'true'
    services:
      postgres:
        image: postgres:15-alpine
        env:
          POSTGRES_USER: postgres
          POSTGRES_PASSWORD: postgres
          POSTGRES_DB: requirements_db_test
        options: >-
          --health-cmd pg_isready
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
        ports:
          - 5432:5432
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: "npm"

      - name: Install dependencies
        run: npm ci

      - name: Run linting
        run: |
          echo "🔍 Running lint checks..."
          npm run lint:api || exit 1
          cd apps/portal && npm run lint || exit 1
          echo "✅ Linting passed"

      - name: Run tests with coverage
        working-directory: apps/api
        env:
          NODE_ENV: test
          DB_HOST: localhost
          DB_PORT: 5432
          DB_USERNAME: postgres
          DB_PASSWORD: postgres
          DB_DATABASE: requirements_db_test
          DB_SYNCHRONIZE: true
          JWT_SECRET: test-jwt-secret-key-min-32-characters-long
          JWT_EXPIRES_IN: 1h
        run: |
          echo "🧪 Running tests..."
          npm run test:cov:check || exit 1
          echo "✅ Tests passed"

      - name: Verify builds
        run: |
          echo "🏗️ Verifying builds..."
          npm run build:api || exit 1
          cd apps/portal && npm run build || exit 1
          echo "✅ All builds successful"

  deploy-to-vps:
    name: Deploy to Hostinger VPS
    runs-on: ubuntu-latest
    needs: [integrity-check]
    if: always() && (needs.integrity-check.result == 'success' || github.event.inputs.skip_tests == 'true')
    environment:
      name: ${{ github.event.inputs.environment || (github.ref == 'refs/heads/main' && 'production' || 'development') }}
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup SSH
        uses: webfactory/ssh-agent@v0.9.0
        with:
          ssh-private-key: ${{ secrets.VPS_SSH_KEY }}
        if: secrets.VPS_SSH_KEY != ''

      - name: Install SSH tools
        run: |
          sudo apt-get update
          sudo apt-get install -y openssh-client sshpass || true

      - name: Test SSH connection
        run: |
          echo "🔐 Testing SSH connection..."
          if [ -n "${{ secrets.VPS_SSH_KEY }}" ]; then
            ssh -o StrictHostKeyChecking=no -o UserKnownHostsFile=/dev/null \
              ${{ env.VPS_USER }}@${{ env.VPS_HOST }} \
              "echo 'SSH connection successful'"
          elif [ -n "${{ secrets.VPS_SSH_PASSWORD }}" ]; then
            sshpass -p '${{ secrets.VPS_SSH_PASSWORD }}' \
              ssh -o StrictHostKeyChecking=no -o UserKnownHostsFile=/dev/null \
              ${{ env.VPS_USER }}@${{ env.VPS_HOST }} \
              "echo 'SSH connection successful'"
          else
            echo "❌ No SSH credentials provided"
            exit 1
          fi

      - name: Create deployment script
        run: |
          cat > deploy-remote.sh << 'DEPLOY_SCRIPT'
          #!/bin/bash
          set -e

          MODULE_NAME="${{ env.MODULE_NAME }}"
          MODULE_PATH="${{ env.MODULE_PATH }}"
          GIT_REPO_URL="${{ github.repositoryUrl }}"
          GIT_BRANCH="${{ github.ref_name }}"
          GIT_SHA="${{ github.sha }}"
          ENVIRONMENT="${{ github.event.inputs.environment || (github.ref == 'refs/heads/main' && 'production' || 'development') }}"

          echo "🚀 Starting deployment..."
          echo "Module: $MODULE_NAME"
          echo "Path: $MODULE_PATH"
          echo "Branch: $GIT_BRANCH"
          echo "Commit: $GIT_SHA"
          echo "Environment: $ENVIRONMENT"

          # Create module directory if it doesn't exist
          mkdir -p $MODULE_PATH
          cd $MODULE_PATH

          # Clone or update repository
          if [ -d ".git" ]; then
            echo "📥 Updating repository..."
            git fetch origin
            git reset --hard origin/$GIT_BRANCH
            git clean -fd
          else
            echo "📥 Cloning repository..."
            git clone -b $GIT_BRANCH $GIT_REPO_URL .
          fi

          # Checkout specific commit
          git checkout $GIT_SHA

          # Create .env file
          echo "⚙️ Creating environment file..."
          cat > .env << EOF
          NODE_ENV=$ENVIRONMENT

          # Database Configuration
          DB_USERNAME=${{ secrets.DB_USERNAME }}
          DB_PASSWORD=${{ secrets.DB_PASSWORD }}
          DB_DATABASE=${{ secrets.DB_DATABASE }}
          DB_PORT=${{ secrets.DB_PORT || '5432' }}
          DB_SYNCHRONIZE=${{ env.ENVIRONMENT == 'development' && 'true' || 'false' }}
          DB_LOGGING=${{ env.ENVIRONMENT == 'development' && 'true' || 'false' }}

          # JWT Configuration
          JWT_SECRET=${{ secrets.JWT_SECRET }}
          JWT_EXPIRES_IN=${{ secrets.JWT_EXPIRES_IN || '1d' }}

          # CORS Configuration
          CORS_ORIGIN=${{ secrets.CORS_ORIGIN || 'http://localhost:4200' }}

          # API Configuration
          API_PORT=${{ secrets.API_PORT || '3000' }}

          # Portal Configuration
          PORTAL_PORT=${{ secrets.PORTAL_PORT || '4200' }}
          NEXT_PUBLIC_API_URL=${{ env.ENVIRONMENT == 'production' && secrets.NEXT_PUBLIC_API_URL_PRODUCTION || secrets.NEXT_PUBLIC_API_URL_DEV }}

          # Monitoring
          ENABLE_MONITORING=true
          METRICS_RETENTION_MS=3600000
          LOG_LEVEL=${{ env.ENVIRONMENT == 'production' && 'info' || 'debug' }}
          EOF

          # Ensure Docker is running
          if ! command -v docker &> /dev/null; then
            echo "❌ Docker is not installed"
            exit 1
          fi

          # Stop existing containers
          echo "🛑 Stopping existing containers..."
          docker-compose down || true

          # Pull latest images if using registry
          if [ -n "${{ secrets.DOCKER_REGISTRY }}" ]; then
            echo "📥 Pulling Docker images..."
            docker-compose pull || true
          fi

          # Build and start services
          echo "🐳 Building and starting services..."
          docker-compose up -d --build

          # Wait for services to be ready
          echo "⏳ Waiting for services to start..."
          sleep 30

          # Run database migrations
          echo "🗄️ Running database migrations..."
          docker-compose exec -T api npm run migration:run || echo "⚠️ Migrations skipped or already applied"

          # Health checks
          echo "🏥 Performing health checks..."

          # Check API
          for i in {1..30}; do
            if docker-compose exec -T api curl -f http://localhost:3000/api/v1/health/liveness > /dev/null 2>&1; then
              echo "✅ API is healthy"
              break
            fi
            echo "Waiting for API... ($i/30)"
            sleep 2
          done

          # Check Portal
          for i in {1..30}; do
            if docker-compose exec -T portal wget --no-verbose --tries=1 --spider http://localhost:4200 > /dev/null 2>&1; then
              echo "✅ Portal is healthy"
              break
            fi
            echo "Waiting for Portal... ($i/30)"
            sleep 2
          done

          # Show service status
          echo "📊 Service Status:"
          docker-compose ps

          echo "✅ Deployment completed successfully!"
          DEPLOY_SCRIPT

          chmod +x deploy-remote.sh

      - name: Copy deployment script to VPS
        run: |
          if [ -n "${{ secrets.VPS_SSH_KEY }}" ]; then
            scp -o StrictHostKeyChecking=no -o UserKnownHostsFile=/dev/null \
              deploy-remote.sh ${{ env.VPS_USER }}@${{ env.VPS_HOST }}:/tmp/deploy-remote.sh
          elif [ -n "${{ secrets.VPS_SSH_PASSWORD }}" ]; then
            sshpass -p '${{ secrets.VPS_SSH_PASSWORD }}' \
              scp -o StrictHostKeyChecking=no -o UserKnownHostsFile=/dev/null \
              deploy-remote.sh ${{ env.VPS_USER }}@${{ env.VPS_HOST }}:/tmp/deploy-remote.sh
          fi

      - name: Execute deployment on VPS
        run: |
          echo "🚀 Executing deployment on VPS..."
          if [ -n "${{ secrets.VPS_SSH_KEY }}" ]; then
            ssh -o StrictHostKeyChecking=no -o UserKnownHostsFile=/dev/null \
              ${{ env.VPS_USER }}@${{ env.VPS_HOST }} \
              "bash /tmp/deploy-remote.sh"
          elif [ -n "${{ secrets.VPS_SSH_PASSWORD }}" ]; then
            sshpass -p '${{ secrets.VPS_SSH_PASSWORD }}' \
              ssh -o StrictHostKeyChecking=no -o UserKnownHostsFile=/dev/null \
              ${{ env.VPS_USER }}@${{ env.VPS_HOST }} \
              "bash /tmp/deploy-remote.sh"
          fi

      - name: Verify deployment
        run: |
          echo "🔍 Verifying deployment..."
          sleep 10

          # Check API endpoint
          API_URL="${{ env.ENVIRONMENT == 'production' && secrets.API_PRODUCTION_URL || secrets.API_DEV_URL }}"
          if [ -n "$API_URL" ]; then
            curl -f "$API_URL/api/v1/health/liveness" || echo "⚠️ API health check failed"
          fi

          # Check Portal endpoint
          PORTAL_URL="${{ env.ENVIRONMENT == 'production' && secrets.PORTAL_PRODUCTION_URL || secrets.PORTAL_DEV_URL }}"
          if [ -n "$PORTAL_URL" ]; then
            curl -f "$PORTAL_URL" || echo "⚠️ Portal health check failed"
          fi

  notify:
    name: Notify Deployment Status
    runs-on: ubuntu-latest
    needs: [deploy-to-vps]
    if: always()
    steps:
      - name: Send notification
        run: |
          if [ "${{ needs.deploy-to-vps.result }}" == "success" ]; then
            echo "✅ Deployment to Hostinger VPS successful!"
            echo "Environment: ${{ github.event.inputs.environment || (github.ref == 'refs/heads/main' && 'production' || 'development') }}"
            echo "Commit: ${{ github.sha }}"
            echo "Branch: ${{ github.ref_name }}"
          else
            echo "❌ Deployment to Hostinger VPS failed!"
            exit 1
          fi
